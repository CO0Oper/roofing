const form=document.querySelector('#demo-form'),success=document.querySelector('#success'),first=form.querySelector('input');form.addEventListener('submit',e=>{e.preventDefault();if(!form.reportValidity())return;form.hidden=true;success.hidden=false;success.focus()});document.querySelector('#reset').addEventListener('click',()=>{form.reset();success.hidden=true;form.hidden=false;form.scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth',block:'center'});first.focus({preventScroll:true})});
